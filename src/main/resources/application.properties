spring.application.name=schematron

# URL for the IBFD collections lookup file used during XML-to-HTML transformation.
# The XSLT doc() call will resolve this at transform time.
# To use the bundled local copy instead, set:
collections.url=classpath:config/collections.xml
#collections.url=https://dtd.ibfd.org/dtd/config/collections.xml
